POSTGRES_DB=qor
POSTGRES_USER=qor
POSTGRES_PASSWORD=qor

# qor-website's event detail page (W17) — Google Maps JavaScript API key.
# Set this in your own .env (gitignored), never commit a real key.
GOOGLE_MAPS_API_KEY=
