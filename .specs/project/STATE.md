# State

**Last Updated:** 2026-09-03
**Current Work:** `qor-mobile`'s Shared KMP module (S1–S11) and Android foundation (A1–A6 — app wiring, NIGHTLIFE-GV component library, BottomNav/EmptyState/ConsentCapture/FormFields) are merged to `main` (PRs #1–#2, see AD-018). A follow-up refactor PR (#3, see AD-019) then moved every hardcoded UI string to Android resources, renamed the org id `br.com.qor` → `br.com.qualorock` across all three modules, changed the app's display name to "Qual o Rock?", and renamed the shared design-token object `NightlifeGvTokens` → `QualORockThemeTokens`. PR #3 merged with a red iOS CI check (caught after the fact, not before) — fixed and documented as AD-020/L-003: the iOS job's hardcoded simulator device name broke on the GitHub runner's actual Xcode/simulator lineup, and `ARCHITECTURE.md` §8.11 now requires explicit `gh pr checks` verification before every merge. A7–A14 (the actual screens + nav-graph wiring) and the entire iOS track (I1–I14) remain. `qor-website`'s home and event-detail pages were restyled per the approved "Cena GV" mockup (PR #3, see AD-017), on top of MVP Core. `qor-website` MVP Core is now fully implemented and merged — W11–W24 (data hooks, all 8 pages, layout/nav, Playwright E2E) shipped in PR #2, see AD-016 — on top of the W1–W10 foundation (PR #1, AD-015). This required building real OTP (one-time-code) auth in `qor-api` from scratch (PRs #19–#21, see AD-016), since the Stitch mocks assumed OTP but the original implementation only had signed-link verification, plus a critical fix (PR #22, see AD-016) making the fan guard's Sanctum SPA cookie auth actually work in a real browser for the first time — the exact same bug class AD-013 fixed for the admin guard, just never yet hit for fans since no fan-facing SPA existed until now. `.specs/tasks/website.md` is now fully implemented. `qor-admin` Monetization UI (AT24–AT34) is merged (PR #5, see AD-014) — Plan CRUD for Super Admin, the organizer plan/usage view, and the publish-quota at-limit gate on event submission are all E2E-verified working in a real browser (`make e2e-admin`), alongside the already-merged MVP Core (AT1–AT23, PRs #3–#4). `qor-admin` now covers every `.specs/tasks/admin.md` task through Milestone 3; P2 (change-plan/upgrade/downgrade/cancel, MON-19–26) remains deferred — no `qor-api` route exists for it yet. Monetization milestone (ROADMAP.md milestone 3) P1 scope (MON-01–MON-18) merged in `qor-api` (PR #14) — see AD-009. MVP Core `api` (PRs #3–#5, #7, #8) and the entire Social & Notifications milestone (PRs #10–#13) are also fully merged — see AD-008. `landingpage` submodule still has no feature work.

---

## Recent Decisions (Last 60 days)

### AD-020: `qor-mobile` iOS CI fixed (dynamic simulator resolution); merge-gate now requires explicit `gh pr checks` (2026-09-03)

**Decision:** Fixed the iOS CI job's `xcodebuild test` failure (`Unable to find a device matching the provided destination specifier { name:iPhone 16 }`) by replacing the hardcoded `-destination` with one resolved at runtime from `xcrun simctl list devices available iOS -j`, and hardened it against GitHub Actions' `${{ }}`-interpolation script-injection anti-pattern by passing the resolved name through an `env:` block instead of directly into the shell string. Shipped as `fix/mobile-ci-ios-simulator-destination` (`qor-mobile` PR #4, 2 commits), reviewed by `review-kmp-android`, merged to `main` (`9b31401`) only after explicitly confirming both CI jobs green via `gh pr checks` — see L-003 for why that verification step is now mandatory.
**Reason:** Discovered while investigating why PR #3 had merged with a red check — the root cause was pure CI-runner drift (GitHub's `macos-15` image's active Xcode version doesn't have an "iPhone 16" simulator registered under that exact name), not an application bug.
**Trade-off:** None of substance. The dynamic resolution picks the *first* available iOS simulator, not necessarily an iPhone specifically (an iPad could theoretically be picked if the runner's device list ever reorders) — flagged by `review-kmp-android` as a low-risk, optional follow-up (filter for `"iPhone" in name`), not fixed here since it's non-blocking and today's actual behavior is correct.
**Impact:** Confirmed via `gh run view` on both the PR and the subsequent `push`-to-`main` run: both `Android + shared (Kotlin)` and `iOS (SwiftUI)` jobs pass. `ARCHITECTURE.md` §8.11 step 4 now explicitly names `gh pr checks <PR>` as the required verification, closing the process gap that let PR #3 merge red in the first place.

### AD-019: `qor-mobile` no-magic-strings cleanup + org id rename to br.com.qualorock, app renamed "Qual o Rock?" (2026-09-03)

**Decision:** Follow-up refactor on top of AD-018's Android foundation, four changes: (1) every hardcoded pt-BR display string/content-description in the Compose component library moved to `res/values/strings.xml`, resolved via `stringResource(...)` — `BottomNavDestination`/`CityFilterStyle` now carry `@StringRes` resource IDs instead of raw strings; (2) `Modifier.testTag(...)` identifiers moved to their own `res/values/strings-test-tag.xml` (`translatable="false"`), referenced directly with no Kotlin `const val` wrapper; (3) org id renamed `br.com.qor` → `br.com.qualorock` across all three modules (Android applicationId/package + directory move, shared's Android namespace, iOS bundle identifier via `xcodegen generate` regeneration) and the app's display name changed "QOR" → "Qual o Rock?" (Android `strings.xml`, iOS `CFBundleDisplayName`); (4) the shared design-token object `NightlifeGvTokens` renamed to `QualORockThemeTokens` (declaration + ~140 call sites). `ARCHITECTURE.md` §14.3 was updated first (root repo, its own commit) to document the `strings.xml`/`strings-test-tag.xml` conventions this PR then follows. Shipped as `refactor/mobile-no-magic-strings-org-rename` (`qor-mobile` PR #3, 7 commits — one per platform boundary per ARCHITECTURE §8.10, including one unavoidable one-commit gap where `androidApp` doesn't compile against the `QualORockThemeTokens` rename's `shared`-only commit alone, closed by the very next commit before pushing), reviewed by `review-kmp-android`, merged to `qor-mobile`'s `main` (`04e074e`).
**Reason:** User request — ARCHITECTURE.md's "no magic numbers/strings" principle wasn't actually followed in `qor-mobile`'s Android UI (which predates this rule's mobile-specific §14.3 documentation), plus an explicit org/app rebrand from "QOR"/`br.com.qor` to "Qual o Rock?"/`br.com.qualorock`.
**Trade-off:** None of substance — a pure refactor, no behavior change. `EventDateBadge.kt`'s pt-BR month abbreviations deliberately stay a plain Kotlin list rather than a string-array resource, since `formatDateBadge`/`formatEventTime` are pure, non-`@Composable`, Context-free functions unit-tested directly; forcing a `Context` through them for a fixed, non-localized value would break that testability for no benefit.
**Impact:** `review-kmp-android`'s pass found no issues — confirmed zero remaining `br.com.qor`/`NightlifeGvTokens` references, correct platform-commit boundaries, no `stringResource(...)` misuse outside a `@Composable` context, and reconfirmed the full gate (`./gradlew clean build test koverVerify :androidApp:koverVerifyDebug detekt`, plus an iOS `xcodebuild` against the new bundle id) green. Re-flagged, not fixed (pre-existing, predates this PR): `qor-mobile` has no `ktlintCheck` Gradle task wired up at all.

### AD-018: `qor-mobile` Android foundation A1–A6 built; STATE.md's stale "no mobile work yet" line corrected (2026-09-03)

**Decision:** Discovered `qor-mobile`'s Shared KMP module (S1–S11, `.specs/tasks/mobile.md`) was already fully merged to `main` via PR #1 before this session — nobody had updated STATE.md/ROADMAP.md after that merge, so they kept saying "mobile has no feature work yet" (the same class of doc-drift AD-008 fixed for `api`). Built the Android track's foundation on top of it, scoped to A1–A6 per user request (not the full A1–A14+I1–I14 remaining scope, which is too large for one session): A1 (Compose Navigation + Koin DI wiring), A2 (NIGHTLIFE-GV Compose component library — `EventCard`, `CityFilterBar`, `GenreTagSet`, gradient CTA buttons, entrance-stagger helper, live-pulse badge), A3 (`BottomNav`), A4 (`EmptyState`/`PlaceholderImage`), A5 (`ConsentCapture`), A6 (form input components). Shipped as `feat/mobile-android-foundation` (7 commits: 6 task commits + one review-fix commit), reviewed by `review-kmp-android`, merged to `qor-mobile`'s `main` (PR #2, `87c39d4`).
**Reason:** User asked to read ARCHITECTURE.md and implement the next mobile tasks; mobile.md's own execution plan sequences A1 → A2 → {A3–A6} before any screen (A7+) can be built.
**Trade-off:** `shared` also had no enforced 80%-coverage CI gate at all despite `kover` being applied since S1 — `mobile-ci.yml` only ran `test`/`detekt`. Closed as a prerequisite: added `koverVerify`/`koverVerifyDebug` rules (min 80%) to both modules, wrote the missing S9 unit tests (`ExerciseDataRight`, `UpdateProfile`, several `UserRepositoryImpl` branches) that a prior session's "Done when" required but never delivered, and excluded genuinely platform-untestable glue (Android/iOS `actual` secure-storage/HTTP-engine implementations, already documented as "Gate: build-verified only" by S8) from the denominator rather than padding it with framework-wiring tests.
**Impact:** 42 `androidApp` tests + `shared`'s suite all passing, both `koverVerify` (shared, 85%+) and `:androidApp:koverVerifyDebug` (80%+) green, `detekt` clean, full `./gradlew build` green. Along the way, found and fixed two real Compose bugs the tests themselves caught: (1) nesting `EventCard`'s whole-card `clickable` around its `MapaCta` CTA caused Compose's semantics-merge to chain both `OnClick` actions together — fixed by making the CTA a sibling of the card-tap region, not a descendant; (2) `BottomNav`'s underline `Modifier.fillMaxWidth()` inside an unweighted `Row` child claimed the entire row's width for the first item, leaving every later tab zero-width and unclickable — fixed with `RowScope.weight(1f)` per item. Also pinned `androidx.navigation:navigation-compose` to 2.9.x (2.10.0+ needs compileSdk 37/AGP 9.1+, incompatible with this repo's AGP 8.13.2 pin) and Robolectric's simulated SDK to 34 (API 36's shadow jar needs JDK 21; this repo is pinned to JDK 17). `review-kmp-android`'s pass caught and fixed one more real gap before merge: `androidApp` itself had no static analysis wired at all (only `shared` applied the `detekt` plugin), and applying it surfaced that `shared`'s `config/detekt/detekt.yml` — built only for S4's domain-boundary check — was being auto-discovered as the default config for any module's plain `detekt` task, which would have wrongly banned `android.*`/`androidx.*` imports throughout `androidApp`; renamed to `domain-boundary.yml` and referenced explicitly from its one real caller, plus a narrowly-scoped `androidApp/config/detekt/detekt.yml` (Composable naming/length exemptions) and two magic-number fixes. Flagged, not fixed: ktlint isn't configured anywhere in `mobile` (pre-existing since S1), and `shared`'s own module-wide `detekt` task returns `NO-SOURCE` (only `detektDomainBoundary` actually lints `shared`) — both worth a follow-up task.

### AD-017: `qor-website` home/event-detail pages restyled — "Cena GV" mockup (2026-09-02)

**Decision:** Restyled `qor-website`'s already-shipped home (`/`) and event-detail (`/eventos/[id]`) pages to match an approved HTML mockup ("Cena GV", after broadway-sings.webflow.io — marquee ticker, horizontal event carousel, full-bleed hero, "Ben Platt card" detail hero), built on the existing NIGHTLIFE-GV tokens. New components: `Footer`, `Marquee`, `EventCarousel`, `CityGrid`, `HeroFeature`, `EventHero`. `CityFilterBar` moved off `/` entirely — the home feed is now a curated, soonest-first, unfiltered-by-city view; `/eventos` (already-built, untouched otherwise) now reads an initial `?city=` param so `CityGrid`'s tiles actually pre-filter it into. Shipped as `feat/website-cena-gv-redesign` (`qor-website` PR #3, 13 commits: 11 TDD tasks + one Task-6 fix round + one final-review fix wave), reviewed by `review-react-web`, merged to `qor-website`'s `main` (`a464f23`).
**Reason:** User approved the mockup and asked to apply it to the real app, with an explicit follow-up that the mockup's animations/transitions be preserved, not simplified away — a visual/behavioral restyle of shipped pages, not new product scope.
**Trade-off:** No fabricated data was introduced to fill the mockup's visual gaps (no venue names, prices, genre labels, or per-city event counts — the real `Event`/`EventDetail` contract's existing gaps, see AD-015/AD-016, are respected as-is, just restyled around). The detail page's `useEventList({city: event?.city})` call fires an unfiltered request (plus a recurring background poll) before the event's city is known, then re-fires filtered — deferred rather than fixed, since a real fix needs `enabled`/`skip` semantics added to the shared `useEventList` hook (used by `/` and `/eventos` too), a separate scoped change. The pre-existing "Ver no Mapa" ticket-button mislabel (AD-015-era `CtaButton` reuse, not introduced here) became more visible once a real "Ver no mapa" map-anchor link was added elsewhere on the same page — also deferred, needs a `ticket` variant/label override on the shared `CtaButton`.
**Impact:** 38 suites / 199 tests passing, lint clean, 97%+ statement coverage on new code. A full whole-branch review (after all 11 scoped task reviews passed individually) caught two real cross-task integration seams neither scoped review could see on its own — `EventCarousel`'s prev/next arrow buttons rendered as empty circles (an SVG with no path), and `HeroFeature`'s "Ver no mapa" link to `/eventos/<id>#map` almost certainly no-op'd, since the client-rendered detail page shows a loading state until the event resolves, so no `#map` element exists yet when the browser tries to hash-scroll — both fixed in a follow-up commit (real chevron icons; a `useEffect` that scrolls to `#map` once `event` loads) and re-verified clean. `review-react-web`'s pre-merge pass found no further issues and independently reconfirmed the fixes.

### AD-016: `qor-website` W11–W24 merged — MVP Core complete, OTP auth built in `qor-api`, two E2E-discovered bugs fixed (2026-09-02)

**Decision:** Shipped the rest of `.specs/tasks/website.md`'s MVP Core: `useEvents`/`useAuth`/`useProfile` hooks (W11–W14), all 8 pages — home, explore, event detail (Google Maps JS API embed), login, signup, email verification, password-recovery wizard, profile (W15–W22) — auth-aware layout/nav (W23), and a Playwright E2E smoke test (W24: browse → filter → event detail → signup → verify → login → profile). Shipped as `feat/website-hooks-pages-layout-e2e` (`qor-website` PR #2), reviewed by `review-react-web`, merged to `main` (`b3509e6`). Building this surfaced that two of the Stitch mocks (email verification, password recovery) assumed OTP (6-digit code) verification, but `qor-api` only had signed-link verification — user confirmed building real OTP end-to-end rather than reshaping the pages to fit the old mechanism, shipped as three sequential `qor-api` PRs: #19 (`OtpVerificationPort`/`OtpAdapter`, new `/auth/email/verify-code` + `/auth/password/verify-code` endpoints), #20 (a fixed, well-known OTP code in `local`/`testing` environments only, so the E2E suite can drive real signup/reset flows without a mail inbox — replacing an initial debug-endpoint design `review-laravel-api` correctly rejected as an unauthenticated secret-disclosure surface), and #21 (a CI-breaking gap in #19/#20: two unit test files, `RegisterFanTest`/`ResetPasswordTest`, were never updated for the new `OtpVerificationPort` constructor dependency, so `main`'s CI was red for roughly two hours between #20 merging and #21 fixing it — caught by checking `gh run list` proactively rather than assuming a green merge, not by CI notifying anyone).
**Reason:** Per ROADMAP.md/AD-015's "next up" — the W1–W10 foundation was merged with no real pages yet; this was the rest of `qor-website`'s MVP Core milestone.
**Trade-off:** Google OAuth login is a disabled placeholder button (no client ID provided yet — same deferral as `qor-admin`'s own admin-side Google gap). `EventCard`'s props (`venueName`→`location`, `genre`/`mapsUrl`/`instagramUrl` all made optional) were reworked because `qor-api`'s public Event/EventDetail JSON has no venue-name field and no resolvable genre name — the same pre-existing genre-list gap flagged in AD-015, now confirmed blocking two clients instead of one.
**Impact:** `qor-api` PR #22 fixed a real, user-facing authentication bug that this session's own E2E test caught: `AuthController::sessionResponse()` (the fan-facing `login()`/`google()` endpoints) only ever issued a mobile bearer token — it never called `Auth::guard('fan-session')->login($model)`, so a website login reported 200 success but every subsequent cookie-based request (`GET /profile`, ...) came back 401 in a real browser. This is the exact same bug class AD-013 already fixed for the admin guard (`admin-session`); the fan-side guard existed in config all along (`config/auth.php`'s `fan-session`, added defensively during AD-013 "for symmetry") but nothing had ever called it, since no fan-facing SPA existed until this session. Fixed identically: `Auth::guard('fan-session')->login($model)` in `sessionResponse()`, plus the matching `TransientToken`-safe `logout()`. Separately, the same E2E test caught a real race condition in `qor-website` itself, in two layers: (1) `app/eventos/[id]/page.tsx` called `useEventDetail(0)` as a placeholder while Next.js 16's async `params` promise was still resolving, and the placeholder's 404 could arrive *after* the real event's 200 and overwrite it (`useEventDetail` now takes `id: number | null` and skips fetching entirely on `null`); (2) `review-react-web`'s pass on the fix caught that the same hook instance is reused across a client-side navigation between two different event ids without remounting, so an in-flight fetch for the old id could still resolve after the new id's and overwrite it — closed with a `latestId` ref guard on every state update inside `refetch()`, plus a regression test forcing the stale response to resolve last. `review-react-web` also flagged (not blocking, tracked as a Todo below) that `/`/`/eventos/[id]` are fully client-rendered with no SSR/`generateMetadata`, so shared event links unfurl with generic site-wide branding instead of the event's own title/flyer — directly undercutting W17's own share feature.

### AD-015: `qor-website` AT1–W10 merged — foundation + NIGHTLIFE-GV design-system library (2026-09-02)

**Decision:** Bootstrapped `qor-website` (previously README-only) from scratch: Next.js App Router + TypeScript + Tailwind v4 scaffold, Docker/CI wiring, a typed cookie-based Sanctum SPA client for `/api/v1` (`lib/api/`), TS mirrors of `qor-api`'s backed enums (`lib/enums/`), and the full NIGHTLIFE-GV component library (`EventCard`, `CityFilterBar`/`GenreTagSet`, CTA buttons/`FormField`, `EmptyState`/`PlaceholderImage`/`LivePulseBadge`, `ConsentCapture`) ported near-verbatim from `design-system.md` §2–4. Shipped as `feat/website-foundation-design-system` (PR #1, 12 commits), reviewed by `review-react-web`, merged to `qor-website`'s `main` (`6a6442a`).
**Reason:** Per ROADMAP.md's "next up" — `qor-website` had zero code despite `qor-api` having MVP Core, Social & Notifications, and Monetization P1 all merged, and `qor-admin` already fully through Milestone 3. W1–W10 was scoped as a first milestone slice (of `.specs/tasks/website.md`'s 24 MVP-Core tasks) sized for one session, mirroring the exact foundation-first split `qor-admin` used for its own AT1–AT10.
**Trade-off:** W11–W24 (data hooks, the actual home/explore/event-detail/auth/profile pages, layout/nav wiring, E2E smoke test) remain undone — `qor-website` still has no real pages, just the component library and a placeholder home page. Uses **Jest + React Testing Library** for unit tests (not Vitest like `qor-admin`) — website.md's own documented tooling choice, a deliberate cross-repo difference, not an inconsistency.
**Impact:** `review-react-web`'s pass found no Critical/High issues (79/79 tests passing, 99.77% coverage, lint/build/audit all clean); one Medium finding fixed before merge — `EventCard`'s `isLive` heuristic was business logic embedded in a component (ARCHITECTURE §1.2 wants that in `lib/`), extracted to `lib/events/isLive.ts` (its `react-hooks/purity` eslint-disable turned out unnecessary once moved, since that rule only targets component/hook bodies). Two Medium findings were left as deliberate, documented follow-ups rather than fixed here: (1) `nightlife-gv.css`'s `@theme` token block is currently unconsumed — every component uses raw Tailwind arbitrary-hex classes instead, matching `design-system.md`'s own hand-written blueprint values; reconciling to theme-derived utility classes is real work better done once W15+'s real pages exist to drive it; (2) `Event.genre_id` has no display-name resolution (`qor-api` still has no genre-list endpoint — the same gap `qor-admin` hit) — `GenreTagSet`'s `GenreTag` already documents this and falls back gracefully for unknown genre names, but W16's real wiring is blocked on it. A TODO was added to `lib/api/http.ts`'s `PUBLIC_PATHS` flagging that `/favoritos` (Milestone 2, W31) needs adding there when built, since that list has no test forcing it to stay current. `api.md` was confirmed stale relative to the real `qor-api` fan-facing routes during this session (missing several endpoints the client now covers) — same class of drift as `qor-admin` hit with its own client.

### AD-014: `qor-admin` AT24–AT34 merged — Monetization UI (Plan CRUD, quota usage, at-limit gate) (2026-09-02)

**Decision:** Shipped `.specs/tasks/admin.md`'s Milestone 3 in full: `usePlans()`/`useOrganizerSubscription()` hooks, `PlanForm`/`PlanTable`/`QuotaUsageWidget` components, Super Admin Plan CRUD pages (`/planos`, `/planos/novo`, `/planos/[id]/editar`, deactivate via the existing `DecisionModal`), the organizer `/assinatura` plan/usage view, "Planos"/"Assinatura" nav items, and a publish-quota at-limit banner + upgrade-prompt path. Shipped as `feat/admin-monetization-ui` (PR #5, 11 commits), reviewed by `review-react-web`, merged to `qor-admin`'s `main` (`cbd7f4f`). AT24 (API client extension) needed no code — `lib/api/client.ts` already covered every Plan/Subscription endpoint, built ahead of schedule during AT4's "every `/api/admin/v1` route" pass.
**Reason:** Per ROADMAP.md's "next up" — `qor-admin` MVP Core (AT1–AT23) was fully merged and E2E-verified; Milestone 3 was the last unimplemented slice of `admin.md`.
**Trade-off:** P2 (change-plan/upgrade/downgrade/cancel, MON-19–23) has no admin UI — `qor-api` has no routed `change-plan`/`cancel` endpoint yet (confirmed via `grep` on `api/routes/api_admin_v1.php`), so `/assinatura` is display-only for now. `PlanForm`'s `publish_quota` is a required integer, not nullable — `qor-api`'s `CreatePlanRequest`/`UpdatePlanRequest` don't accept the domain model's "unlimited" (`null`) case yet, flagged below as a Todo rather than worked around client-side.
**Impact:** `review-react-web`'s first pass caught a real Critical/High pair before merge: the publish-quota at-limit gate had initially been wired to the event *create*/*edit* pages, but `qor-api`'s `CheckAndIncrementQuota` only runs inside `SubmitEventForReview` (`POST /events/{id}/submit`) — never on create or edit — so the gate was blocking actions that were never actually quota-gated (an organizer at their limit couldn't even fix a typo on an already-published event) while leaving the one place a `quota_exceeded` response can really happen (the event-list page's "Enviar para revisão" button) with zero error handling. Fixed by moving the at-limit banner and the reactive `ApiError.code === "quota_exceeded"` catch (a new field added to `ApiError`, sourced from `qor-api`'s 422 payload per AD-009) to `app/eventos/page.tsx`'s submit action, and reverting the create/edit pages to their pre-fix state. The review also caught a dead-end upgrade-prompt link (pointed at `/assinatura`, which is itself display-only and can't upgrade anything) — removed until a real `qor-landingpage` plan-comparison URL exists. Final state: 232/232 tests passing, ~94-95% coverage (gate: 80%), lint clean, build succeeds; both `e2e/mvp-core-smoke.spec.ts` and the new `e2e/monetization-smoke.spec.ts` pass individually (they share a pre-existing `throttle:qor-auth` rate limiter in `qor-api` that trips only when both specs run back-to-back with multiple admin logins in the same invocation — unrelated to this PR, not fixed here).

### AD-013: `qor-api` — the admin guard's Sanctum SPA cookie auth never actually worked until now (2026-09-01)

**Decision:** Fixed three compounding gaps so `Auth::guard('admin')`-protected routes actually authenticate a real browser via session cookie, not just the bearer `token` `qor-admin`'s client deliberately never reads: (1) `bootstrap/app.php` now prepends Sanctum's `EnsureFrontendRequestsAreStateful` to the `api` middleware group — it was never applied, since routes are registered manually via `Route::middleware('api')` in a `then` callback rather than `withRouting()`'s `api:` shorthand, which is what normally auto-adds it; (2) `AdminAuthController::login()` now calls `Auth::guard('admin-session')->login($model)` — it previously never called any guard's `->login()` at all, only minted an unused Sanctum token; (3) added real session-backed `fan-session`/`admin-session` guards (`config/auth.php`) and pointed `config('sanctum.guard')` (was `['web']`, undefined in this project) at them. Shipped as `fix/admin-spa-session-auth` (PR #18), reviewed by `review-laravel-api`.
**Reason:** Discovered building `qor-admin`'s AT23 E2E smoke test (the first time anyone exercised the real browser cookie-session login flow — every prior test used `Sanctum::actingAs()`, bypassing HTTP-level session/cookie mechanics). Verified via a live cookie round-trip against the running dev stack and via the Playwright E2E spec, which failed until all three gaps were closed.
**Trade-off:** Touches core auth infrastructure/config across three files at once — a bigger, riskier diff than a typical fix, but the three gaps were genuinely interdependent (fixing any one alone still left the flow broken).
**Impact:** `review-laravel-api` caught a real blocker before merge: `AdminAuthController::logout()` called `currentAccessToken()->delete()` unconditionally, which fatals for a session-cookie request (resolves to Sanctum's `TransientToken`, no `delete()`) — now guarded, and `logout()` also actually ends the session (`Auth::guard('admin-session')->logout()` + session invalidation), which it never did even before this PR. The review also correctly flagged that the original PHPUnit test coverage didn't actually exercise the bug (passed even with the middleware fix reverted, due to `SESSION_DRIVER=array` sharing in-process state across requests in one test method) — an HTTP-level test asserting a session cookie is set on login was added and independently verified to fail-without/pass-with the fix. `fan-session` is unused by any controller yet (no fan-facing SPA built) but was added for symmetry — the same landmine would otherwise await whichever session builds `qor-website`'s fan cookie auth next. Flagged, not fixed here (informational, pre-existing): a stale `EnsureFanIdentity` docblock, and no `config/cors.php` (ARCHITECTURE §7 wants an explicit origin allowlist; this PR's stateful-domain checks are the first place it's actually load-bearing).

### AD-012: `qor-api` admin session/profile gap-fill — `GET /me`, `/venues/me`, `/promoters/me` (2026-09-01)

**Decision:** Added three read-only endpoints to `qor-api`'s admin surface: `GET /api/admin/v1/me` (returns the authenticated admin's account plus a new `account_type: 'super_admin'|'venue_admin'|'promoter'`, derived via the existing `OrganizerIdentityResolver` and a new `Domain\Admin\Enum\AccountType`), and `GET /api/admin/v1/venues/me` / `GET /api/admin/v1/promoters/me` (read-only counterparts to the existing `PATCH` routes). Shipped as `feat/api-admin-session-me` (PR #16, `qor-api` `main` at `5e9f50b`), reviewed by `review-laravel-api`.
**Reason:** Discovered while starting `qor-admin` AT11–AT23 (hooks/pages): `qor-admin` is a Sanctum httpOnly-cookie SPA with no client-side session persistence (ARCHITECTURE §2 — no localStorage), so on every page load/refresh it had no way to learn who's logged in, whether a non-super-admin is a Venue or Promoter, or a venue's own address — the last one needed for AT20's venue-default-address-vs-promoter-manual-location event-form branching. No endpoint exposed any of this. User confirmed (asked directly): fix it at the source rather than fake session state client-side or ship AT20 without real address defaulting.
**Trade-off:** A cross-repo detour mid-`qor-admin`-session — small (3 endpoints, no new domain logic, pure read-projection reuse) but still its own branch/PR/review cycle before the `qor-admin` work it unblocks could start.
**Impact:** `review-laravel-api`'s pass found no Critical/High issues; two Medium/Low findings fixed before merge: extracted `findOwnVenueModel()`/`findOwnPromoterModel()` so `show()`/`update()` share the ownership lookup instead of duplicating it, and added `Domain\Admin\Enum\AccountType` instead of leaking `Domain\Event\Enum\EventCreatedByType`'s vocabulary into the admin session response. Fixing the review's "redundant local test route" finding surfaced a real pre-existing bug: `VenueControllerTest`/`PromoterControllerTest`'s `register` tests target `/api/admin/v1/venues`/`/promoters` via a local route override, while the actual production route is `/venues/register`/`/promoters/register` — a latent mismatch predating this PR (those tests have never exercised the real route). Left as-is and documented inline; not fixed here (out of scope for a session/profile gap-fill) — flagged below as a Todo. Also flagged, informational only: `AdminAuthController::login()` still returns a plaintext bearer `token` in its JSON body, which this PR's own rationale (SPA-cookie-only auth) makes look increasingly out of place; and `VenuePolicy`/`PromoterPolicy` remain unwired by any controller.

### AD-011: `qor-admin` AT1–AT10 merged — foundation + Corona design-system component library (2026-09-01)

**Decision:** `qor-admin` (previously README-only) now has its Next.js App Router + TypeScript + Tailwind v4 scaffold, Docker/CI wiring, a typed cookie-based Sanctum SPA client for every `/api/admin/v1` route (`lib/api/`), TS mirrors of `qor-api`'s backed enums (`lib/enums/`), and the full Corona (`design-system-admin.md`) component library — Sidebar/Topbar, StatCard/DonutWidget/StatusPill/DataTable, Button/Badge/ProgressBar, RegistrationForm/EventForm, ConsentCapture/DecisionModal. Shipped as `feat/admin-foundation-design-system` (PR #3, 12 commits), reviewed by `review-react-web`, merged to `qor-admin`'s `main` (`dc70da0`).
**Reason:** Per ROADMAP.md/STATE.md's "next up" — `qor-admin` had zero UI despite `qor-api` having MVP Core, Social & Notifications, and Monetization P1 all merged. AT1–AT10 was scoped as a first milestone slice (of `.specs/tasks/admin.md`'s 34 tasks) sized for one session with real per-task verification and commits.
**Trade-off:** AT11–AT34 (API hooks, the actual approval-queue/registration/event/dashboard pages, role-aware layout wiring, E2E smoke test, and all of Milestone 3's Monetization UI) remain undone — `qor-admin` still has no real pages, just the component library and a placeholder home page.
**Impact:** `review-react-web`'s pass caught two real gaps before merge: the 80%-coverage gate was configured but not enforced in CI (fixed — `test:coverage` now the CI job, coverage raised from 69.91% to 95.57% functions via added tests for every previously-untested `lib/api/client.ts` endpoint and `EventForm`/`RegistrationForm` branches), and a duplicated semantic-color hex map across `DonutWidget`/`ProgressBar` (deduped into `components/design-system/colors.ts`). One review claim — that React Hook Form + Zod is "the project's binding form-handling convention" — was checked against `.specs/**`/`CLAUDE.md`/`design-system-admin.md` and found undocumented anywhere; not acted on. Separately, this session also fixed a real ambiguity in the root Makefile: `up`/`down` were the only aggregate targets and `test` was hardcoded to the `api` service with no per-submodule convention — ARCHITECTURE.md §8.1 now documents a `test-<service>`/`lint-<service>`/`build-<service>` + aggregate `test` convention, and the root Makefile/docker-compose.yml were updated to match (`admin` service added, port 3001). `AdminAuthController::login()`'s response includes a `token` field that the new API client deliberately never reads or persists — ARCHITECTURE.md §2 mandates httpOnly-cookie auth for `qor-admin`, so the session cookie is the only credential this client relies on; flagged as an existing `qor-api` response-shape quirk, not something this PR's scope covered fixing.

### AD-010: `qor-admin` design source switched from "Corona React" to "Corona Tailwind" (2026-09-01)

**Decision:** `design-system-admin.md` now traces to the BootstrapDash **"Corona Tailwind — Modern Vertical"** demo (`https://demo.bootstrapdash.com/corona-tailwind/themes/modern-vertical/`) instead of the earlier "Corona React — Modern Vertical" capture. Re-navigated live (Dashboard, Widgets, Buttons, Badges, Modals, Progress Bar, Tables, Login) with the same `getComputedStyle`-measurement method as before. `.specs/tasks/admin.md`'s quick index was synced to match.
**Reason:** User preference change mid-project — asked for the admin panel rebuilt from this sibling template instead, including its interaction behaviors and motion.
**Trade-off:** Every measured token had to be re-verified rather than assumed shared across the two demos. Most held (semantic accent hex values, surface colors, sidebar width, status-pill color mapping all reconfirmed identical), but several changed materially: no custom Google Font (system font stack instead of Rubik, confirmed with user), a uniform 6px card/button/badge radius (was a 2–4px mix), and a much flatter motion language — buttons/inputs have no hover/focus transition at all in this build (was 150ms ease-in-out), only the sidebar's expand/collapse animates (300ms ease-in-out). The login page also gained a photo background and a second social-login button (Google, alongside Facebook) — both still omitted per ARCHITECTURE §2 (no social login).
**Impact:** Docs-only change — `qor-admin` has no application code yet (empty scaffold), so no migration was needed. `design-system-admin.md` is a full rewrite; `.specs/tasks/admin.md`'s quick index and "Resolved" paragraph were updated to match. The modal open/close transition could not be measured live (toggle didn't trigger during capture) — flagged in both files as unmeasured, to be re-checked when `admin.md` AT9's `DecisionModal` is actually built rather than assumed from the superseded capture.

### AD-009: Monetization P1 (MON-01-18) merged — Plan/Subscription domain, quota enforcement (2026-08-29)

**Decision:** `qor-api`'s Monetization milestone P1 scope shipped as a single PR (`feat/api-monetization`, PR #14, 26 tasks/commits): `Plan`/`Subscription` domain entities, repositories, all Billing use cases, targeted extensions to `DecideAccountApproval` (grants the default free plan on approval) and `SubmitEventForReview` (enforces the publish quota) — `DecideEventApproval` verified untouched per MON-09. Public/admin HTTP endpoints, a monthly quota-reset schedule, and a seeder round it out. P2 (MON-19–26: upgrade/downgrade, cancellation, annual billing enforcement) is deferred; the `annual_price`/`billing_cycle` schema fields already exist so no future migration is needed for it.
**Reason:** Per ROADMAP.md's milestone sequencing — Monetization is next after MVP Core and Social & Notifications (AD-008). `spec.md`/`design.md` were already content-complete from an earlier session; this pass added `tasks.md` and executed it.
**Trade-off:** API-only, same as every milestone so far — `qor-admin` (Plan CRUD UI, usage widget) and `qor-landingpage` (plan comparison page) still don't exist.
**Impact:** `review-laravel-api`'s pass on PR #14 caught one High and three Medium findings before merge, all fixed: (1) a check-then-increment race condition in the quota logic, fixed with an atomic `SubscriptionRepository::incrementUsageIfUnderQuota()` using a transaction + row lock; (2) a missing unique constraint on `subscriptions(subscribable_type, subscribable_id)`; (3) a non-atomic multi-repository write sequence in `DecideAccountApproval`, fixed by introducing a new `Domain/Shared/TransactionManager` port (this codebase's first cross-repository transaction abstraction — prior `DB::transaction()` usage was always internal to a single Eloquent adapter method); (4) a generic `InvalidArgumentException` for the quota-exceeded case, replaced with a dedicated `QuotaExceeded` exception mapped to a machine-readable `code: quota_exceeded` for the future `qor-admin` upgrade-prompt UI. Final state: 588 tests passing, PHPStan level 9 clean.

### AD-008: Root docs/submodule pointer had drifted 4 PRs behind `api`'s actual `main` (2026-08-29)

**Decision:** Resynced the root repo's `api` submodule pointer (was pinned at `a2a5a39`, checked-out HEAD was already `a930ff7`) and rewrote STATE.md/ROADMAP.md to reflect what's actually merged: Phase 4 (event lifecycle: Edit/Duplicate/Cancel + admin tools, PR #7), admin login (ADMIN-28-30, PR #8), Postman docs (PR #9), and the full Social & Notifications milestone across sub-phases 5a–5d (PRs #10–#13: favorites, friend request/accept/remove/list, friends-interested, share, social feed, notification preferences, nearby-reminder/regional-publish detectors, event-changed/cancelled handling).
**Reason:** Nobody committed the root submodule-pointer bump or updated STATE.md/ROADMAP.md after PRs #7–#13 merged in `api` — the docs kept describing a state 4 PRs stale, which made "what phase are we on" unanswerable from the docs alone.
**Trade-off:** None — this is a pure resync, no code or scope change. `favorites-social/spec.md` and `notifications/spec.md`'s Requirement Traceability tables still read "In Design" for every row despite the code being merged; a full per-requirement reconciliation pass for those two tables is flagged as a Todo rather than done here (would require verifying each MON-style ID against actual test coverage, not just commit messages).
**Impact:** ROADMAP.md now shows MVP Core and Social & Notifications as DONE (`qor-api` P1, UI not started in any client submodule) and Monetization as the active milestone. This session proceeds into Monetization's Tasks/Execute phases on that corrected basis.

### AD-007: Phase 3 split into three sequential PRs, two infra gaps filled framework-natively (2026-08-28)

**Decision:** `qor-api` Phase 3 (T23–T43) ships as three sequential branches/PRs — Event Discovery (merged, PR #3), Auth & Fan Profile (merged, PR #4), Venue/Promoter Admin (in progress). Two gaps Phase 2 left open are filled with Laravel-native mechanisms behind domain ports rather than custom builds: `AdminAccountRepository` for the admin login row created alongside Venue/Promoter registration, and email-verification/password-reset via signed URLs + the stock `password_reset_tokens` broker (`EmailVerificationPort`/`PasswordResetPort`).
**Reason:** Keeps each PR reviewable and mergeable independently (milestones are sequential per CLAUDE.md); avoids inventing new custom mechanisms where Laravel already has a fitting one.
**Trade-off:** Three review passes instead of one; the admin-login-row gap-fill adds one more domain entity/repository pair not in the original Phase 2 scope.
**Impact:** PR #4's `review-laravel-api` pass caught and fixed two issues before merge: `EloquentUserRepository::delete()` was a plain soft-delete without PII scrub (violated the LGPD "right to be forgotten" design decision — fixed), and the email-verification link TTL was hardcoded instead of config-driven (fixed, added `qor.auth.email_verification_ttl_minutes`). PR #5 (Venue/Promoter Admin, T34–T43) added a second gap-fill (`AdminAccountRepository`, per AD-007's plan) plus `ApprovalOutcome::SuspensionLifted` (missing from Phase 2 despite ADMIN-27 needing it), an `EnsureSuperAdmin` guard restricting the two approval-queue controllers, and a `DomainException`→422 global exception mapping (illegal `Event` state-machine transitions previously had no HTTP mapping and would have 500'd). Its review pass fixed one issue: `RegisterPromoter` could leave a dangling `AdminAccount` row if the `Promoter` entity's own field validation failed after the account was already saved — `RegisterVenue` already guarded against this via a throwaway pre-validation construction; `RegisterPromoter` was missing the same guard. `EditEvent`/`DuplicateEvent`/`CancelEvent` and their `PATCH`/duplicate/cancel routes are deliberately deferred to Phase 4 (T44) — not implemented in PR #5. CAPTCHA (ARCHITECTURE §13.2) remains deferred — see Todos.

### AD-001: Local dev orchestration via docker-compose + Makefile (2026-08-27)

**Decision:** Root repo provides a `docker-compose.yml` and `Makefile` to orchestrate local dev services across submodules.
**Reason:** Each submodule (api, mobile, admin, website, landingpage) needs a consistent way to spin up its dependencies without duplicating orchestration logic per repo.
**Trade-off:** Adds a root-level infra surface that must stay in sync as submodules add new services.
**Impact:** New services (e.g. databases, object storage) get wired into the root compose file as they're introduced.

### AD-002: PostgreSQL confirmed as the database (2026-08-27)

**Decision:** PostgreSQL is the system-of-record database for `qor-api`.
**Reason:** Confirmed and documented in ARCHITECTURE.md as the resolved choice for the project.
**Trade-off:** None recorded beyond the standard Postgres operational profile.
**Impact:** All Eloquent migrations, repository adapters, and CI test setup target Postgres.

### AD-003: Sanctum dual-guard split + versioned route groups (2026-08-27)

**Decision:** `qor-api` uses two separate Sanctum guards (fan vs. venue/promoter/admin) and a hard route-group boundary — `/api/v1/...` for end users, `/api/admin/v1/...` for admin — never a shared credential space.
**Reason:** Per ARCHITECTURE.md's auth model; fans and organizers/admins have distinct trust boundaries and must never share tokens or route groups.
**Trade-off:** More boilerplate (two guards, two route groups) than a single unified auth scheme.
**Impact:** Every new endpoint must be classified into one of the two guards/route groups at creation time — established in api PR #1 (`feat/mvp-core`).

### AD-004: MinIO as local S3-compatible object store (2026-08-28)

**Decision:** Root `docker-compose.yml` adds a MinIO service for local dev, standing in for S3.
**Reason:** `qor-api`'s new `S3UploadAdapter` (api PR #2) needs an S3-compatible target for local development without requiring real AWS credentials.
**Trade-off:** MinIO's API surface isn't a perfect match for S3 — edge cases may diverge in production.
**Impact:** Local dev and CI can exercise upload flows without cloud dependencies.

### AD-005: Clean Architecture domain layer landed for core entities (2026-08-28)

**Decision:** `qor-api`'s domain layer (entities, repository interfaces + Eloquent adapters, policies) now covers User, Venue, Promoter, Event (with state-machine enforcement), EventPromoter pivot, ApprovalDecision, and ConsentRecord.
**Reason:** Per CLAUDE.md's mandatory Clean Architecture rule — domain layer must have zero framework dependency; this is the first substantial slice landing that structure (api PR #2, `feat/api-domain-repositories-policies`).
**Trade-off:** More indirection (interfaces + adapters) than calling Eloquent models directly.
**Impact:** CI green at merge: 86 tests, 98.6% coverage, PHPStan level 9, `composer audit` clean. Sets the pattern future domain entities should follow.

### AD-006: Reviewer subagents required before submodule PR merge (2026-08-28)

**Decision:** `.claude/agents/` now defines `review-laravel-api`, `review-react-web`, `review-kmp-android`, `review-ios-swift` — one must run against a submodule's PR before it's merged.
**Reason:** Per CLAUDE.md's "Review-before-merge" rule; each submodule has its own stack and conventions that a single generic reviewer wouldn't catch consistently.
**Trade-off:** Adds a manual gate step between PR-open and merge in every submodule.
**Impact:** Fixes found in review are applied silently, with no code comments referencing the review.

---

## Lessons Learned (continued)

### L-003: `qor-mobile` PR #3 merged with a red iOS CI check — `gh pr checks` is now a mandatory, explicit merge step (2026-09-03)

**Context:** PR #3 (AD-019) was merged 72 seconds after its iOS CI job failed (`gh run view 33767875809` — `xcodebuild test: error: Unable to find a device matching the provided destination specifier { name:iPhone 16 }`). The merge went ahead on the `review-kmp-android` subagent's in-session "PASS" verdict alone; `gh pr checks` was never actually run to confirm real CI status, even though `ARCHITECTURE.md` §8.11 already said checks must pass before merge — the rule existed but wasn't followed because "how to verify" wasn't spelled out.
**Solution:** Root-caused and fixed the underlying CI flake (PR #4): the iOS job's `-destination 'platform=iOS Simulator,name=iPhone 16,OS=latest'` was hardcoded against a device name that doesn't exist on the GitHub-hosted `macos-15` runner's active Xcode/simulator lineup — replaced with a step that resolves the first available iOS Simulator name at runtime via `xcrun simctl list devices available iOS -j`, so it survives future runner-image/Xcode-version drift instead of breaking again. `ARCHITECTURE.md` §8.11 step 4 now explicitly requires `gh pr checks <PR>` before merge, not an inferred/assumed status.
**Prevents:** Never merge a submodule PR without running `gh pr checks <PR>` yourself immediately before merging, regardless of how confident a reviewer subagent's verdict sounds — a subagent's review is about code correctness, not CI/runner health, and the two can and did diverge.

### L-002: Cross-repository atomicity now has an established pattern — `Domain/Shared/TransactionManager` (2026-08-29)

**Context:** `DecideAccountApproval` (Monetization P1, AD-009) needed three writes across three repositories to succeed or fail atomically; the codebase's only prior transaction usage (`DB::transaction()` in `EloquentUserFavoriteGenreRepository`) was internal to one Eloquent adapter and couldn't span repositories from the Domain layer without leaking `Illuminate\*` imports there.
**Solution:** Added `src/Domain/Shared/TransactionManager.php` (a plain `run(callable): mixed` interface) + `src/Infrastructure/Persistence/LaravelTransactionManager.php` (the `DB::transaction()` adapter), bound in `AppServiceProvider` alongside the other `Domain/Shared` ports (`FileUploadPort`, `PasswordHasher`).
**Prevents:** The next use case needing atomicity across more than one repository should inject `TransactionManager` rather than reaching for `DB::transaction()` directly (which isn't available from the Domain layer anyway) or inventing a parallel mechanism.

---

## Active Blockers

_None currently open._

---

## Lessons Learned

### L-001: Mass-assignment vulnerability caught before api PR #1 merge (2026-08-27)

**Context:** During `qor-api`'s initial MVP scaffold (`feat/mvp-core`), a model exposed privilege fields to mass-assignment, and a dead frontend scaffold was left in the tree.
**Problem:** Both were caught during review, not before the branch was opened — mass-assignment on privilege fields is a real escalation risk if merged.
**Solution:** Fixed in a dedicated commit (`13c5d88`) before merge; `c4f5673` merged clean.
**Prevents:** `review-laravel-api` should explicitly check `$fillable`/`$guarded` on any model touching roles, approval status, or account state — this is now a standing check, not a one-off catch.

---

## Quick Tasks Completed

| #   | Description | Date | Commit | Status |
| --- | ------------ | ---- | ------ | ------ |

_None yet — no ad-hoc quick-mode tasks recorded._

---

## Deferred Ideas

_None captured yet._

---

## Todos

- [ ] `landingpage` submodule has no feature work yet (README-only) — next up per ROADMAP.md alongside the rest of `mobile`. (`admin`, `website`, and `mobile`'s Shared+Android-foundation now do — see AD-011–AD-015, AD-018.)
- [ ] `mobile`'s Android screens (A7–A14: login/signup/verify/recover/home/explore/detail/profile + nav-graph wiring) and the entire iOS track (I1–I14, mirrors Android exactly) remain — see AD-018. Next Android session picks up at A7.
- [ ] `mobile` has no ktlint configured anywhere (`./gradlew ktlintCheck` doesn't exist) — pre-existing since S1, flagged during AD-018's `review-kmp-android` pass, not fixed there.
- [ ] `mobile/shared`'s module-wide `detekt` task returns `NO-SOURCE` — only the narrow `detektDomainBoundary` task (S4) actually lints `shared`'s domain package; the rest of `shared/data` and platform source sets aren't checked by the general ruleset. Flagged during AD-018's review, not fixed there (pre-existing KMP/detekt wiring gap from S1–S11).
- [x] CLAUDE.md's repository-topology note ("none of which are checked out yet") was stale — all 5 submodules are already checked out. Fixed 2026-08-28.
- [ ] CAPTCHA (ARCHITECTURE §13.2) not implemented on any public unauthenticated form (fan signup, password-reset request, venue/promoter registration) — no provider chosen yet, not in PRD's resolved decisions. Needs a follow-up task once a vendor is picked.
- [ ] Google ID token is trusted client-side (`POST /api/v1/auth/google` accepts `google_id`/`email`/`name` as-is) — server-side verification via Google's token library isn't wired up. Flagged in `GoogleAuthRequest`'s docblock (api PR #4).
- [x] Venue/Promoter/Super Admin login (ADMIN-28–30) — Tasks/Execute closed 2026-08-28 on branch `feat/api-admin-login` (T1–T4, T6): `AuthenticateAdmin` use case, admin `LoginRequest`, `AdminAuthController` (`POST /api/admin/v1/auth/login`, `POST /api/admin/v1/auth/logout`) wired into the existing `admin` guard/`AdminAccountRepository`/`AdminUserModel`/`EnsureAdminIdentity` infra from PR #5. 354 tests passing, 98.5% coverage, phpstan clean. T7 (`review-laravel-api`) pending PR open.
- [ ] Admin password recovery (forgot/reset password for Venue Admin/Promoter/Super Admin) is unspecified — `auth-fan-profile`'s `ResetPassword`/`password_reset_tokens` flow only covers fans. Not yet requested; flagged alongside the login gap for a future Specify pass if needed.
- [x] `qor-admin` Next.js scaffold + Corona design-system component library — AT1–AT10 merged (PR #3, see AD-011) 2026-09-01. AT11–AT34 (hooks, actual pages, layout wiring, E2E, Monetization UI) remain — next `qor-admin` session should pick up at AT11.
- [ ] `favorites-social/spec.md` and `notifications/spec.md` Requirement Traceability tables still show every row as "In Design" despite both features being fully merged in `api` (PRs #10–#13, see AD-008) — needs a per-requirement reconciliation pass against actual code/tests, not a bulk status flip.
- [ ] `qor-mobile`/`qor-website` have no UI for Favorites & Social or Notifications yet — those milestones are API-only so far.
- [ ] `api`'s `VenueControllerTest`/`PromoterControllerTest` `register` tests hit `/api/admin/v1/venues`/`/promoters` via a local test-only route override, while the real production route is `/venues/register`/`/promoters/register` — those tests have never exercised the actual route (found while addressing `review-laravel-api`'s findings on PR #16, see AD-012). Needs its own fix pass: correct the test URLs (or the local override) to match production, then confirm they still pass against the real route.
- [ ] `AdminAuthController::login()` still returns a plaintext bearer `token` in its JSON body even though `qor-admin`'s client deliberately never reads it (ARCHITECTURE §2 mandates SPA-cookie-only auth for the admin guard) — flagged again during AD-012's review; a candidate for removal once confirmed nothing else depends on it.
- [ ] `VenuePolicy`/`PromoterPolicy` remain unwired by any controller in `qor-api` (dead code per ARCHITECTURE §8.4) — flagged during AD-012's review, not fixed there (out of scope for a session/profile gap-fill).
- [ ] `EnsureFanIdentity`'s docblock is stale about Sanctum's cookie-vs-token provider scoping — flagged during AD-013's review (PR #18), not fixed there (out of scope for the admin-guard session fix).
- [ ] No `config/cors.php` in `qor-api` — ARCHITECTURE §7 calls for an explicit origin allowlist; AD-013's stateful-domain checks (PR #18) are the first place this has actually been load-bearing. Needs a follow-up pass once `qor-website`/`qor-landingpage` origins are known.
- [x] `qor-admin` AT11–AT23 (hooks, pages, layout wiring, E2E) — merged (PR #4) 2026-09-01, `review-react-web` findings addressed, E2E-verified end-to-end. See AD-013 for the api-side prerequisite fixes this required.
- [ ] `qor-api` has no genre-list endpoint at all (neither `/api/v1` nor `/api/admin/v1`) — `qor-admin`'s `EventForm` (`components/design-system/EventForm.tsx`) takes a raw numeric `genre_id` input instead of a fetched picker as a direct result, and `qor-website`'s `GenreTagSet`/`EventCard` (AD-015) can only resolve a genre's display name/color for 5 hardcoded names, with a neutral fallback otherwise. Flagged during PR #4's `review-react-web` pass, re-confirmed still open during AD-015. Needs a small `qor-api` endpoint before either client's real genre wiring (`qor-admin`'s picker, `qor-website`'s W16 explore-page filter) can be built.
- [x] `qor-admin` AT24–AT34 (Monetization UI: Plan CRUD, quota usage, at-limit gate) — merged (PR #5, see AD-014) 2026-09-02, `review-react-web` findings addressed, E2E-verified end-to-end. `.specs/tasks/admin.md` is now fully implemented through Milestone 3.
- [x] `qor-website` W1–W10 (foundation + NIGHTLIFE-GV design-system library) — merged (PR #1, see AD-015) 2026-09-02, `review-react-web` findings addressed.
- [x] `qor-website` W11–W24 (data hooks, all 8 pages, layout/nav, Playwright E2E) — merged (PR #2, see AD-016) 2026-09-02, `review-react-web` findings addressed, E2E-verified end-to-end. `.specs/tasks/website.md` is now fully implemented.
- [ ] `qor-website`'s `/` and `/eventos/[id]` are fully client-rendered (`"use client"` + `useEffect` fetch) with no SSR/`generateMetadata` — the server's first response has only the site-wide static title/description and a loading skeleton, so a shared event link (W17's own `navigator.share`/clipboard feature) unfurls on WhatsApp/Instagram with generic branding instead of the event's title/flyer, and search engines get no crawlable per-event content. Flagged during AD-016's review; needs moving the initial fetch into a Server Component with `generateMetadata`, keeping only the interactive bits (share button, city filter) as client islands.
- [ ] `qor-api`'s old signed-link email-verification/password-reset routes (`GET /auth/email/verify/{id}/{hash}`, `GET /auth/email/pending/verify/{id}/{hash}`, and the underlying `EmailVerificationPort::send()`/`PasswordResetPort::sendResetLink()`) are now dead code from the fan-facing client's perspective — `RegisterFan`/`ResetPassword::requestReset()` switched to issuing OTP codes instead (AD-016). Kept rather than deleted since the routes still work and nothing has confirmed no other caller depends on them; candidate for a future cleanup pass once confirmed unused.
- [ ] `nightlife-gv.css`'s `@theme` token block (AD-015) is currently unconsumed — every `qor-website` component uses raw Tailwind arbitrary-hex classes matching `design-system.md`'s own hand-written blueprint values instead of the generated `bg-nightlife-*`/`text-nightlife-*` utilities. Flagged during AD-015's review as worth reconciling once real pages (W15+) exist to drive it — not fixed in the foundation PR.
- [ ] `qor-website`'s `lib/api/http.ts` `PUBLIC_PATHS` needs `/favoritos` added when Milestone 2's W31 builds that page (favorites-social's spec allows anonymous browsing there, only the favorite-toggle action needs auth) — flagged during AD-015's review as a TODO comment in that file; no test currently forces this list to stay current.
- [ ] `qor-api`'s `CreatePlanRequest`/`UpdatePlanRequest` validate `publish_quota` as a required, non-nullable integer, but the Plan/Subscription domain model treats `null` as "unlimited" — that state isn't reachable through the admin Plan CRUD form. Flagged during AD-014's review; needs a small `qor-api` fix (`nullable` on that field) before `qor-admin`'s `PlanForm` can offer an "unlimited" option.
- [ ] No `change-plan`/`cancel` endpoints exist in `qor-api` for Monetization P2 (MON-19–23) — `qor-admin`'s `/assinatura` page is display-only pending that work.
- [ ] `qor-admin`'s `QuotaUsageWidget` upgrade-prompt link has nowhere to point yet — no `qor-landingpage` plan-comparison URL exists (that submodule has no feature work at all). Needs revisiting once `qor-landingpage`'s Monetization milestone page exists.
- [x] `qor-website` home/event-detail "Cena GV" redesign — merged (PR #3, see AD-017) 2026-09-02, `review-react-web` findings addressed (none needed). Full whole-branch review caught and a follow-up commit fixed two integration-only bugs (empty carousel arrows, dead `#map` deep-link) that no single scoped task review could see.
- [ ] `qor-website`'s `app/eventos/[id]/page.tsx` calls `useEventList({city: event?.city})` unconditionally, so every detail-page view (including cancelled/ended banner-only ones) fires one recurring background `/events` poll that's discarded until the event's city resolves (AD-017). Needs `enabled`/`skip` semantics added to the shared `useEventList` hook (`hooks/useEvents.ts`, also used by `/` and `/eventos`) — a separate scoped change, not done in AD-017 since it touches a hook shared across pages.
- [ ] `qor-website`'s ticket-purchase `CtaButton` (`variant="map"`, reused for `ticket_url`) is labeled "Ver no Mapa" even though it opens a checkout/ticketing link, not a map — a pre-existing mislabel (predates AD-017) now more visibly contradictory since AD-017 added a real "Ver no mapa" map-anchor link elsewhere on the same detail page. Needs a `ticket` variant or a label-override prop added to `CtaButton`.

---

## Preferences

**Model Guidance Shown:** never
